/*
* Routes to address components to views
*/
var Routes = {
	/**
	 * the correct syntax is
	 *
	 * name_of_group : array_of_values passed from the MVC framework
	 */
	"HomePage" 		: ["home_view"]
};
/*
* Layers for every component
*/ 
var Layers = {
	/**
	 * the correct syntax is
	 *
	 * name_of_group : array_of_components defined as extensions of pageElements
	 */
	"HomePage"		: ["Header", "Footer"],
}