/* Namespace container for decorators
* Some examples included
*/
var Decorators = {
	
	/* DECORATOR : Concrete Decorator
	*
	*/
	Header : PageElement.extend({
		bindEvents : function() {
	
		}
	}),

	/* DECORATOR : Concrete Decorator
	*
	*/
	Footer : PageElement.extend({
		bindEvents : function() {
		
			
		}
	}),

	/* DECORATOR : Concrete Decorator
	*
	*/
	Sidebar : PageElement.extend({
		bindEvents : function() {
	
		}
	}),

}